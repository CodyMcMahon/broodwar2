function loadTestStuff(){
  map.units[map.units.length] = mktestbuilding(250,250,3);
  map.units[map.units.length] = mktestbuilding(350,350,3);
  map.units[map.units.length] = mktestbuilding(450,450,3);
  map.units[map.units.length] = mktestbuilding(550,350,3);
  map.units[map.units.length] = mktestbuilding(650,250,3);
  map.units[map.units.length] = mktestbuilding(850,250,3);
  map.units[map.units.length] = mktestbuilding(850,350,3);
  map.units[map.units.length] = mktestbuilding(850,450,3);
  map.units[map.units.length] = mktestbuilding(950,250,3);
  map.units[map.units.length] = mktestbuilding(950,450,3);
  /*
  map.units[map.units.length] = mktestbuilding(900,400,3);
  map.units[map.units.length] = mktestbuilding(900,400,3);
  map.units[map.units.length] = mktestbuilding(900,400,3);
  map.units[map.units.length] = mktestbuilding(900,400,3);
  map.units[map.units.length] = mktestbuilding(900,400,3);
  map.units[map.units.length] = mktestbuilding(900,400,3);
  map.units[map.units.length] = mktestbuilding(900,400,3);
  map.units[map.units.length] = mktestbuilding(900,400,3);
  map.units[map.units.length] = mktestbuilding(900,400,3);
  map.units[map.units.length] = mktestbuilding(900,400,3);
  map.units[map.units.length] = mktestbuilding(900,400,3);
  */
  
}

/*
     if((y > t.y - t.size/2 >   
     &&  y + height > zodiacKiller.y + t.size/2) 
     && (x > t.x + t.size / 2 
     && x+width < t.x - t.size / 2)){
      zodiacKiller.x = newx;
     }
     if((newy - 50 > person.y + 50 
     || newy + 50 < person.y - 50 ) 
     || (zodiacKiller.x - 50 > person.x + 50 
     || zodiacKiller.x + 50 < person.x - 50)){
      zodiacKiller.y = newy;
     }
*/